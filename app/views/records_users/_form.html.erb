<%= form_for(@records_user) do |f| %>
  <% if @records_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@records_user.errors.count, "error") %> prohibited this records_user from being saved:</h2>

      <ul>
      <% @records_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :album_title %>
    <%= f.text_field :album_title %>
  </div>

  <div class="field">
    <%= f.label :artist %>
    <%= f.text_field :artist %>
  </div>

  <div class="field">
    <%= f.label :year %>
    <%= f.number_field :year %>
  </div>


  <div class="field">
    <%= f.label :condition %>
    <%= f.select(:condition, ["Unopened", "Brand New", "Excellent", "Very Good", "Good", "Acceptable", "Some Damage"]) %>
  </div>

  <div class="field">
    <%= f.label :rating %>
    <%= f.number_field :rating %>
  </div>

  <div class="field">
    <%= f.label :favorite %>
    <%= f.check_box :favorite %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
